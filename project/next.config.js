/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['us.supreme.com', 'cdn-images.farfetch-contents.com', 'www.kickscrew.com']
    }
}

module.exports = nextConfig
